import Cascade from "../Cascade";

Cascade.start(() => {
});

class UserManagement
{
    static isValidUser(username:string, password:string, callback:(isValid:boolean, isReadOnly:boolean) => void)
    {

    }
}
module.exports = UserManagement;

